# 平成プリクラ機 音声アナウンス 原稿

現在は仮としてAI音声（edge-tts / ja-JP-NanamiNeural、rate+15%、pitch+15Hz）で生成しています。
実際に録音し直す場合は、同じファイル名で書き出して `audio/` フォルダ内のファイルを差し替えてください
（コード側はファイル名を参照しているだけなので、差し替えのみでOKです）。

演者イメージ：当時のゲームセンター店員／プリクラ機の呼び込みのような、ハキハキ元気な女性の声。
早口で明るく、語尾を少し上げるイメージ。

| ファイル名 | 再生タイミング | セリフ |
|---|---|---|
| `01_start.mp3` | タイトル画面でモードを選んだ瞬間 | 「レッツ、プリクラ！」 |
| `02_select_curtain.mp3` | シールデザイン選択画面に入った瞬間 | 「シールのデザインをえらんでね！」 |
| `03_select_frame.mp3` | 選択画面に入って約2.7秒後（まだ選択画面にいる場合のみ） | 「フレームも決めたら、OKを押してね！」 |
| `intro_shot1.mp3` | 1枚目の撮影カウントダウン開始時（前置き） | 「1枚目、いくよー！」 |
| `intro_shot2.mp3` | 2枚目の撮影カウントダウン開始時（前置き） | 「2枚目！かわいく決めて！」 |
| `intro_shot3.mp3` | 3枚目の撮影カウントダウン開始時（前置き） | 「3枚目！もっと寄って！」 |
| `intro_shot4.mp3` | 4枚目（最後）の撮影カウントダウン開始時（前置き） | 「最後の1枚だよ！」 |
| `count_3.mp3` | 上記の前置きが終わった直後（4回とも共通） | 「3」 |
| `count_2.mp3` | 「3」の再生が終わった直後 | 「2」 |
| `count_1.mp3` | 「2」の再生が終わった直後 | 「1」 |
| `count_hai.mp3` | 「1」の再生が終わった直後。この音声が終わると同時に撮影される | 「はい！」 |
| `13_beauty.mp3` | 盛り調整画面に入った瞬間 | 「盛りチェック！美肌もデカ目も、自由自在だよ！」 |
| `08_deco_start.mp3` | 落書き画面に入った瞬間 | 「らくがきタイム、スタート！制限時間は3分だよ！」 |
| `09_time_warning.mp3` | 残り時間が10秒を切った瞬間（1回だけ） | 「あと10秒！お急ぎください！」 |
| `10_timeup.mp3` | 制限時間が0になった瞬間 | 「はい、タイムアップ！」 |
| `11_finish.mp3` | 「できあがり！」ボタン→確認で「うん！これでOK♪」を選んだ瞬間 | 「できあがり！」 |
| `12_save.mp3` | 保存画面が表示された瞬間（タイムアップ／できあがり共通） | 「らくがき、かんせーい！保存して、プリントは係の人にお願いしてね！」 |

## 撮影カウントダウンの仕組み（重要）
以前は「1枚目、いくよー！3、2、1、はい！」を1本の音声にしていましたが、前置きの長さが毎回違うため
画面の「3・2・1・ハイ！」表示とズレる問題がありました。そのため **前置き（`intro_shotN`）と
「3」「2」「1」「はい！」（`count_3`〜`count_hai`）を別ファイルに分割** し、コード側は
各音声の再生完了（`ended`イベント）を待ってから次の数字を表示する方式に変更しています。
これにより、音声の長さがどう変わっても表示とボイスは自動的にズレなくなります。
録音し直すときも `SOUND_DURATION` のような秒数調整は一切不要で、ファイルを差し替えるだけでOKです。
`count_3`〜`count_hai` は4回の撮影すべてで共通利用しているので、1セット録るだけで済みます。

## 録音時の注意
- 各ファイルはセリフ単体のみ。現在の音源は ffmpeg で **前後の無音カット＋ラウドネス正規化
  （loudnorm I=-16）** を適用済みです。録音し直した場合も同じ処理をかけると音量が揃います：
  `ffmpeg -i in.mp3 -af "silenceremove=start_periods=1:start_threshold=-45dB:start_silence=0.05,areverse,silenceremove=start_periods=1:start_threshold=-45dB:start_silence=0.08,areverse,loudnorm=I=-16:TP=-1.5:LRA=11,alimiter=limit=0.95" out.mp3`
- 特にカウントダウン（count_3〜count_hai）は無音を詰めるほど実機らしいテンポになります
  （表示側が音声の終わりに追従するので、長さ調整は不要です）。
- ファイル形式はmp3のまま同名で上書きすれば差し替え完了です。

## 追加ボイス13本（2026-08-12・実機考証ボイスパック）

era-designerのリサーチ（平成実機は「若い女性声・ハイテンション・タメ口の掛け声連発」）に基づく追加分。
生成: edge-tts / ja-JP-NanamiNeural、**rate+18%、pitch+25Hz**（既存の+15%/+15Hzより一段明るめ。
テンション優先だが同一話者・近接パラメータで既存音声とトーンの連続性を確保）。
既存分と同じffmpeg処理（前後無音カット＋loudnorm I=-16）適用済み。差し替えは同名上書きでOK。

| ファイル名 | 再生タイミング | セリフ |
|---|---|---|
| `attract_call.mp3` | 待機（アトラクト）画面の呼び込み | 「プリクラ撮ってこ〜！めっちゃ盛れるよ〜！」 |
| `course_select_v2.mp3` | コース選択画面 | 「アップで撮る？全身で撮る？えらんで〜！」 |
| `pose_01.mp3` | 撮影ごとのポーズ提案1 | 「顔、寄せて寄せて〜！」 |
| `pose_02.mp3` | ポーズ提案2 | 「ピースピース！」 |
| `pose_03.mp3` | ポーズ提案3 | 「変顔、いっちゃう？！」 |
| `pose_04.mp3` | ポーズ提案4 | 「あごに手〜！かわいく！」 |
| `pose_05.mp3` | ポーズ提案5 | 「ジャンプしてみて！せーの！」 |
| `pose_06.mp3` | ポーズ提案6（最終カット） | 「ラスト！決めポーズ！」 |
| `ok_check.mp3` | 撮影直後の確認 | 「この画像でオッケー？！」 |
| `moriage_select.mp3` | 盛れ度選択画面 | 「どれくらい盛る？ナチュラル？しっかり盛っちゃう？」 |
| `doodle_owaru.mp3` | 落書き「おわる」押下時 | 「らくがき、おしまい！さいこ〜！」 |
| `doodle_halftime.mp3` | 落書き残り時間の中間通知 | 「残り、はんぶんだよ〜！いそげいそげ〜！」 |
| `print_out.mp3` | 排出演出 | 「できあがり〜！シール、とりにきてね！」 |

## 追加ボイス27本（2026-08-14・新機能向け 追加ボイスパック2）

8/12以降に増えた画面・操作のうち、**音声が付いていなかった場面**を埋める分。
ファイル名の接頭辞で使い分ける: **`heisei_` = 平成モード専用 / `reiwa_` = 令和モード専用 /
接頭辞なし = 両モード共通**。同じ場面でも平成と令和で言い回しを分けてあるので、
結線側は `state.mode` で出し分ける。

生成: edge-tts / ja-JP-NanamiNeural（既存と同一話者）。
- 平成用: **rate +18% / pitch +25Hz**（既存の追加13本と完全に同じ＝当時のハイテンション）
- 令和用: **rate +6% / pitch +10Hz**（同じ声のまま落ち着かせた。「令和は夢のプリクラ機」の
  やわらかいトーン。話者は変えていないので平成⇄令和で人が入れ替わって聞こえることはない）
- 共通: **rate +12% / pitch +18Hz**（どちらの画面で鳴っても浮かない中間）

ffmpegの後処理は**全27本とも既存と完全に同一**（前後無音カット＋loudnorm I=-16＋リミッター）。
実測 -14.4〜-16.0 LUFS で既存分（-14.3〜-15.9 LUFS）と揃っている。
既存mp3は1本も上書きしていない。

### 平成モード専用（7本）

| ファイル名 | 使う場面 | セリフ |
|---|---|---|
| `heisei_deco_gate.mp3` | `#deco-start-gate` が出た瞬間（`startDecoScreen()` 末尾）。平成は写真えらびUIが出ないので、この1本だけでよい | 「らくがきの じゅんび、いい？ ボタンおしたら、３分スタートだよー！」 |
| `heisei_deco_photo_switch.mp3` | `#btn-deco-start` 押下後、`decoStart`（08_deco_start）の**あと**に続けて。写真拡大表示とサムネイル切り替えの案内 | 「しゃしんは１まいずつ、大きくかけるよ！したのサムネイルで、つぎのしゃしんにチェンジ！」 |
| `heisei_layout_gate.mp3` | `showHeiseiLayoutGate()` で分割選択ゲートを出した瞬間 | 「できあがり！さいごに、なんぶんかつで もってかえる？えらんでー！」 |
| `heisei_korokoro.mp3` | 落書き画面でスタンプを**その回はじめて**選んだとき（`setTool('stamp'/'dstamp')` の初回のみ） | 「スタンプはね、タップで１こ！なぞると コロコロって、れんぞくで おせるよー！」 |
| `heisei_camera_wait.mp3` | `startCamera()` の `getUserMedia` を待っている間 | 「カメラ、じゅんび中ー！ちょっとだけ まっててね！」 |
| `heisei_camera_ready.mp3` | カメラのプレビューが出た直後（`previewLoop()` 開始時・撮影スタート前） | 「カメラ、オッケー！ポーズ きまったら、さつえいスタート おしてー！」 |
| `heisei_thanks.mp3` | `#btn-restart`（もう一回あそぶ）を押してタイトルへ戻る瞬間 | 「ありがとねー！また プリクラ とりに きてねー！」 |

### 令和モード専用（13本）

| ファイル名 | 使う場面 | セリフ |
|---|---|---|
| `reiwa_deco_gate.mp3` | `#deco-start-gate` が出た瞬間 | 「らくがき、はじめよっか。準備ができたら、スタートを押してね。」 |
| `reiwa_photo_pick.mp3` | 同ゲートの写真えらび（`buildPhotoPick()`／令和のみ表示）。`reiwa_deco_gate` の**あと**に続けて | 「シールにのせる写真は、タップでえらべるよ。えらんだ順に、ならぶからね。」 |
| `reiwa_deco_photo_switch.mp3` | `#btn-deco-start` 押下後、`decoStart` の**あと**に続けて | 「写真は１枚ずつ、大きく描けるよ。下のサムネイルか、よこスワイプで切りかえてね。」 |
| `reiwa_layout_gate.mp3` | ⚠️ 令和の分割えらびは**撮影後ではなく選択画面（`#sel-layout`）の中**にある。`#sel-layout` が画面に入ったとき、または `#layout-list` を最初に触ったときに鳴らす | 「分割は、２枚ワイドから１６分割まで。持ち帰りたい形をえらんでね。」 |
| `reiwa_moriage_level.mp3` | 盛り画面に入って `13_beauty` が終わったあと。盛れ感レベル（80/100/120%）の説明 | 「盛れ感レベルは三だんかい。ナチュラルも、しっかり盛るのも、えらべるよ。」 |
| `reiwa_makeup_history.mp3` | `#btn-makeup-history` を**表示した**とき（`startBeautyScreen()` でボタンが出る分岐）。押下時ではない | 「まえの盛れを再現、できるよ。ボタンひとつで、４枚ぜんぶに反映されるからね。」 |
| `reiwa_beauty_parts.mp3` | 盛り画面のスライダーを最初に触ったとき（小鼻・涙袋・脚長の新項目の案内） | 「小鼻も、涙ぶくろも、脚ながも。こまかいところまで、思いのままだよ。」 |
| `reiwa_tool_ugokasu.mp3` | 落書き編集ツール `setTool('edit')`（`#btn-tool-edit`「うごかす」）の初回選択時 | 「うごかす、をえらぶと、はったスタンプを あとから動かせるよ。」 |
| `reiwa_korokoro.mp3` | スタンプの初回選択時（`heisei_korokoro` の令和版） | 「スタンプは、タップで１こ。なぞると、コロコロって連なって押せるよ。」 |
| `reiwa_camera_wait.mp3` | `startCamera()` の `getUserMedia` 待ち | 「カメラを準備してるよ。すこしだけ、待っててね。」 |
| `reiwa_camera_ready.mp3` | プレビュー開始直後 | 「カメラ、準備できたよ。ポーズが決まったら、撮影スタートを押してね。」 |
| `reiwa_id_photo.mp3` | プリント画面で `#btn-download-id`（証明プリ・令和のみ表示）を押した瞬間 | 「証明プリも、保存できるよ。あそび用だから、ほんものの証明写真には使えないよ。」 |
| `reiwa_thanks.mp3` | `#btn-restart` でタイトルへ戻る瞬間 | 「ありがとう。またあそびに来てね。」 |

### 両モード共通（7本・接頭辞なし）

| ファイル名 | 使う場面 | セリフ |
|---|---|---|
| `save_guide.mp3` | `deliverImage()` の① `navigator.share` を呼んで共有シートが出る瞬間（`showSaveToast('📤 「画像を保存」を…')` と同じ位置） | 「ほぞんメニューが出るよ。画像を保存、をえらんでね！」 |
| `save_success.mp3` | share の `then`（＝保存メニューに渡せた）／PC系のダウンロード成功時 | 「ほぞん、できたよ！カメラロールを見てみてね！」 |
| `save_longpress.mp3` | `showSaveFallback()`（長押し保存モーダルを出した瞬間） | 「がぞうを ながおしして、しゃしんに追加、をえらんでね！」 |
| `save_retry.mp3` | share の `AbortError`（客が共有シートをやめただけ）。責めない一言 | 「だいじょうぶ。もういちど おせば、ほぞんできるよ！」 |
| `save_error.mp3` | `deliverImage()` の画像生成に失敗したとき（`lastSaveRoute = 'error'`） | 「がぞうが うまく作れなかったみたい。もういちど おしてみてね。」 |
| `camera_error.mp3` | `startCamera()` の catch（`#cam-error` にメッセージを出す瞬間）。**焦らせない落ち着いたトーン** | 「カメラが つかえないみたい。あわてなくて だいじょうぶ。かかりの人を よんでね。」 |
| `resume_offer.mp3` | `offerResume()` で `#resume-modal`（つづきから／さいしょから）を出した瞬間 | 「まえの らくがきが のこっているよ。つづきから はじめる？」 |

### 結線するときの注意（音羽 → 工藤）

1. **案内ボイスは `playAnnounce()` を通す。** `playSound()` で鳴らすと、この27本は
   既存の案内ボイスと重なる。`playAnnounce` は1本チャンネルで前の案内を必ず止める。
2. **「◯◯のあとに続けて」と書いた組は `queueAnnounce()` で繋ぐ。**
   `playAnnounce` を2回続けて呼ぶと1本目が即座に切れる。
   例: `playAnnounce('decoStart'); queueAnnounce(() => playAnnounce('heiseiDecoPhotoSwitch'), soundDurationMs('decoStart') + 250);`
3. **初回だけ鳴らす系（korokoro / ugokasu / beautyParts）はフラグで1回に絞る。**
   道具を持ち替えるたびに喋ると3分の落書きタイムが説明で埋まる。
   フラグは `#btn-restart`（もう一回あそぶ）でリセットする。
4. **`SOUND_FILES` に追加しないと `soundAvailable` の未着スキップが効かない。**
   キー名は `heisei_deco_gate` → `heiseiDecoGate` のようにキャメルケースで。
   `sw.js` のプリキャッシュ一覧にも27本を足すこと（足さないとオフライン時だけ無音になる）。
5. **`ok_check.mp3` が結線漏れしている**（2026-08-14 音羽が発見）。8/12に納品した13本のうち
   これだけ `SOUND_FILES` にも `sw.js` にも入っていない。撮影直後の確認（「この画像でオッケー？！」）で
   鳴らす想定のファイルなので、今回の結線のついでに拾ってほしい。

## 効果音（SE）
`se_tap.mp3`（タッチ音）/ `se_decide.mp3`（決定音）/ `se_shutter.mp3`（シャッター音）は
numpyで合成したオリジナル音源です。差し替えは同名上書きでOK。

## BGM（3曲・オリジナル自作曲）
- `bgm_title.mp3`: タイトル画面用アトラクトポップ（128BPM・約15秒ループ）。最初のタッチで再生開始
- `bgm.mp3`: 平成モード用ユーロビート（152BPM・約13秒ループ）。パラパラ世代のコギャル文化を意識
- `bgm_reiwa.mp3`: 令和モード用ローファイ（72BPM・約27秒ループ）。ii-V進行+テープ揺れ+ビニールノイズ
すべてPython(numpy)による自作曲のためライセンス問題なし。同名mp3の差し替えで変更可能。
